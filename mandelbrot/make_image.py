import numpy as np
import matplotlib.pyplot as plt

fig, ax = plt.subplots(ncols=2, figsize=(20,10))

# cmap = 'magma'

# ax[0].imshow(img, cmap=cmap)

# ax[0].imshow(np.sqrt(img), cmap=cmap)

# plt.imshow(img, cmap='magma')
# plt.show()
